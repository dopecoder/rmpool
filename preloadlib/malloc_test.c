#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    // int i;
    // const int limit = 1;
    // void *ptr[limit];

    void *result = malloc(1024 * 10);
    // printf("Res : %lx\n", result);

    // printf("%s\n", "malloc_test: (main) called");
    // for (i = 0; i < limit; ++i)
    // {
    //     ptr[i] = malloc(10000 * i);
    // }
    // for (i = 0; i < limit; ++i)
    // {
    //     free(ptr[i]);
    // }
    return 0;
}